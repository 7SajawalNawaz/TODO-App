<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="css/Styles.css" />
    <title>TODO App</title>
  </head>
  <body>
    <div class="table-container">
      <h2 class="todo-title">TODO List</h2>
      <div>
        <a class="btn" href="addtodo">Add Todo</a>
      </div>
      <br />

      <table>
        <tr>
          <th>Title</th>
          <th>Description</th>
          <th>Created at</th>
          <th>Updated at</th>
          <th>Action</th>
        </tr>
        <!-- <tr>
          <% for(let i=0 ; i< todos.length ; i++){ %>
          <td><%= todos[i].title %></td>
          <td><%= todos[i].desc %></td>
          <td><%= todos[i].createdAt %></td>
          <td><%= todos[i].updatedAt %></td>
          <td>
            <a class="btn" href="/updatetodo">Edit</a>
            <a class="btn" href="/deletetodo">Delete</a>
          </td>
          <% } %>
        </tr> -->

        <% todos.forEach(todo => { %>
          <tr>
              <td data-label="Title"><%= todo.title %></td>
              <td data-label="Description"><%= todo.desc %></td>
              <td data-label="Created at"><%= todo.createdAt.toLocaleString() %></td>
              <td data-label="Updated at"><%= todo.updatedAt.toLocaleString() %></td>
              <td data-label="Action">
                 
                  <a class="btn" href="/updatetodo?id=<%= todo._id %>">Edit</a>

                  <a class="btn5" href="deleteTodo?id=<%= todo._id %>">Delete</a>
              </td>
          </tr>
          <% }) %>
          </tr>
          
      </table>
    </div>
  </body>
</html>
